本次按“方案 A：家属直接登录同一个患者账号”完成调整。

已改内容：
1. pages/patient/profile/profile.vue
   - 进入页面时并行拉取 /patient/profile、/patient/home、/patient/daily-measurements/today。
   - 优先使用 /patient/daily-measurements/today 的最新体征回填本地 daily_metrics_latest 与 daily_metrics_timeseries。
   - 若 today 接口没有可用数据，则回退使用 /patient/home 返回的 metrics。
   - 去掉右上角“+”入口，不再引导到本地模拟的家属/关联账号页面。

2. pages/patient/healthFile/healthFile.vue
   - 去掉右上角“+”入口。
   - 增加与 profile.vue 一致的后端回填逻辑，保证另一台手机登录同一患者账号后，也能看到后端最新体征。

3. pages/patient/profile/accountadd/accountadd.vue
   - 停用原来基于 uni.setStorageSync('linkedAccounts') 的本地模拟逻辑。
   - 改为说明页，提示当前项目采用“同一患者账号多端登录”的方案。

当前推荐测试方式：
1. 手机 A 登录患者账号，连接硬件或手动录入健康指标，并确认后端保存成功。
2. 手机 B 登录同一个患者账号。
3. 打开“我的”页或“健康档案”页，检查是否能看到后端最新体征。

说明：
- 这次改动没有删除 accountadd 页面路由，只是停用了原先的伪关联逻辑，避免误导。
- 若未来要升级为“家属独立账号 + 患者关联”，建议在后端补关系表和操作人字段后再重做。
